@page
@model CustomerOrderItem.Pages.OrderItems.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Order Items</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrderItem1[0].ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrderItem1[0].OrderID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrderItem1[0].CustomerID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrderItem1[0].ProductID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerOrderItem1[0].Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.CustomerOrderItem1)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OrderID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CustomerID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
